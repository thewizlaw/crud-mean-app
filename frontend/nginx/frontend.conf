# frontend/nginx/frontend.conf
server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve Angular client and fallback to index.html for SPA routes
  location / {
    try_files $uri $uri/ /index.html;
  }
}





